function roomSortOrder(inputArray, inputSort) {
  console.log('order')

  // var inputSort = inputSort.sort;
  // var tempRooms = inputArray.concat()
  // if (!inputSort) {
  //   inputSort = 'id'
  // }
  // var reg = /[^0-9]/
  // function compare(a, b) {
  //   if (+a[inputSort].replace(reg, '') < +b[inputSort].replace(reg, '')) {
  //     return -1
  //   }
  //   if (+a[inputSort].replace(reg, '') > +b[inputSort].replace(reg, '')) {
  //     return 1
  //   }
  //   return 0
  // }
  // tempRooms = tempRooms.sort(compare)

  // return tempRooms
}

export default roomSortOrder
